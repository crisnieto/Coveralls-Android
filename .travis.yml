language: android

android:
  components:
    - build-tools-23.0.1
    - android-23

before_script:
    - echo no | android create avd --skin WXGA800 --force -n test -t "Google Inc.:Google APIs:23"
    - emulator -avd test -no-skin -no-audio -no-window &
    - android-wait-for-emulator
    - adb shell input keyevent 82 &

script:
    - ./gradlew testBlueDebugUnitTestCoverage
